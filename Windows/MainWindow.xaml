<Window x:Class="StickyNoteApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StickyNoteApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="350" Background="#FF464646"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        WindowStyle="None">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition  Height="10"/>
            <RowDefinition  Height="40"/>
            <RowDefinition  Height="30"/>
            <RowDefinition  Height="*"/>
            <RowDefinition  Height="30"/>
            <RowDefinition  Height="30"/>
        </Grid.RowDefinitions>
        
        <!--Top Window Toolbar-->
        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="-" Width="30" Height="30" Background="{x:Null}" Foreground="White"/>
            <Button Content="X" Click="Close_Click" Width="30" Height="30" Background="{x:Null}" Foreground="White" Margin="10,0,0,0"/>
        </StackPanel>
        <!--Note Display Panel-->
        <ItemsControl Grid.Row="3" Grid.Column="1" ItemsSource="{Binding Notes}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Title}" Margin="5" FontSize="16" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Label Content="Sticky Notes" Grid.Row="1" Grid.Column="1" Foreground="White" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Left" />
        <Button Content="Create Note" Grid.Column="1" Grid.Row="4" Width="150" Click="Click_CreateNote"/>
    </Grid>
</Window>
