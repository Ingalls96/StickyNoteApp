<Window x:Class="StickyNoteApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StickyNoteApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="350" Background="#FF232327"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        WindowStyle="None">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition  Height="10"/>
            <RowDefinition  Height="40"/>
            <RowDefinition  Height="30"/>
            <RowDefinition  Height="*"/>
            <RowDefinition  Height="30"/>
            <RowDefinition  Height="30"/>
        </Grid.RowDefinitions>
        
        <!--Top Window Toolbar-->
        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="🗕" ToolTip="Minimize" Click="Minimize_Click" Width="30" Height="30" Background="{x:Null}" Foreground="White"/>
            <Button Content="🗙" ToolTip="Close Program" Click="Close_Click" Width="30" Height="30" Background="{x:Null}" Foreground="White" Margin="10,0,0,0"/>
        </StackPanel>
        <!--Note Display Panel-->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Row="3" Grid.Column="1">
            <ItemsControl Grid.Row="3" Grid.Column="1" ItemsSource="{Binding Notes}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="{Binding Title}" Margin="10,0,0,10" FontSize="16" Height="30" Width="250"
                                    HorizontalAlignment="Left"
                                    Background="{Binding BackgroundColor}"
                                    Click="OpenNote_Click"
                                    Tag="{Binding}" BorderBrush="Black">
                            </Button>
                            <Button Content="🗑" ToolTip="Delete" HorizontalAlignment="Center" Width="30" Margin="10,0,0,10"
                                    Height="30" VerticalAlignment="Center" Background="Transparent" Foreground="White"
                                    Click="Delete_Click" Tag="{Binding}">
                            </Button>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <Label Content="Sticky Notes" Grid.Row="1" Grid.Column="1" Foreground="White" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Left" />
        <Button Content="Create Note" Grid.Column="1" Grid.Row="4" Width="150" Click="Click_CreateNote"/>
    </Grid>
</Window>
